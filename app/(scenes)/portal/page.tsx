"use client";

import { Canvas } from "app/(scenes)/canvas";
import { PortalScene } from "./scene";

export default function Page() {
  return (
    <Canvas>
      <PortalScene />
    </Canvas>
  );
}
